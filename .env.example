# API Keys - Bu anahtarları ilgili platformlardan temin edin / Obtain these keys from the respective platforms
# OpenAI (DALL-E) API key - https://platform.openai.com/
DALLE_API_KEY=your_dalle_api_key_here

# Google Gemini API key - https://ai.google.dev/
GEMINI_API_KEY=your_gemini_api_key_here

# Google Cloud Text-to-Speech
# Bu değerin, Google Cloud konsolundan indirdiğiniz JSON kimlik dosyasının MUTLAK yolunu içermesi gerekir
# This MUST contain the ABSOLUTE path to your JSON credentials file downloaded from Google Cloud Console
# Örnek / Example: /home/user/SleepyStories/.google-cloud/google-credentials.json
GOOGLE_APPLICATION_CREDENTIALS=path_to_your_credentials_file.json

# Ek Yapılandırma / Additional Configuration
# Aşağıdaki değerler opsiyoneldir / The following values are optional

# Port settings (default: 5000)
# PORT=5000

# Log level (default: info) - options: error, warn, info, verbose, debug
# LOG_LEVEL=info